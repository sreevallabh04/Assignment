# =================================
# FRONTEND ENVIRONMENT CONFIGURATION
# =================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control

# API Configuration
# Backend API URL (development)
REACT_APP_API_URL=http://localhost:5000

# Socket.IO Configuration
# Backend Socket.IO URL (development)
REACT_APP_SOCKET_URL=http://localhost:5000

# =================================
# PRODUCTION ENVIRONMENT VARIABLES
# =================================
# For production deployment, use these variables:

# Production API URL
# REACT_APP_API_URL=https://your-backend-api-url.com

# Production Socket.IO URL
# REACT_APP_SOCKET_URL=https://your-backend-api-url.com

# =================================
# OPTIONAL CONFIGURATION
# =================================
# These are optional and have defaults in the code

# Application Title
# REACT_APP_TITLE=TaskFlow

# Theme Configuration
# REACT_APP_THEME=default

# =================================
# SECURITY NOTES
# =================================
# 1. Only add REACT_APP_ prefixed variables (React requirement)
# 2. Never store sensitive data in frontend environment variables
# 3. Frontend .env files are publicly accessible in the browser
# 4. Use different URLs for development and production
# 5. Ensure HTTPS is used in production URLs 